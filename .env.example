# Copy to .env.local (apps/web) and packages/database/.env — never commit real values.

# --- Database ---------------------------------------------------------------
DATABASE_URL="postgresql://dentalos:dentalos@localhost:5432/dentalos_dev?schema=public"

# --- App ----------------------------------------------------------------
NODE_ENV="development"
APP_URL="http://localhost:3000"

# --- Auth (decision pending — see ARCHITECTURE.md "Decisions to take") ------
AUTH_SECRET="change-me-generate-a-real-secret"

# --- Integration abstractions (Phase 5+, wired through provider adapters) ---
# STORAGE_PROVIDER=local|s3|azure-blob
# EMAIL_PROVIDER=postmark|ses|sendgrid
# SMS_PROVIDER=twilio|other
# PAYMENT_PROVIDER=stripe|datatrans|other
# ESIGNATURE_PROVIDER=skribble|other
